{
  "name": "stonegate",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npx serve public -l 5000 --no-clipboard",
    "test": "echo \"No tests yet\" && exit 0"
  },
  "devDependencies": {
    "serve": "^14.2.6"
  }
}
